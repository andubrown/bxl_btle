# ios/Podfile
platform :ios, '12.0'

ENV['COCOAPODS_DISABLE_STATS'] = 'true'

require File.join(File.dirname(__FILE__), 'Flutter', 'podhelper.rb')

target 'Runner' do
  use_frameworks!
  use_modular_headers!
  
  install_all_flutter_pods File.dirname(File.realpath(__FILE__))
end
